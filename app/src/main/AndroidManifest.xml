<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.amon.seasoncoffee02">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/mainicon"
        android:label="Сезонные напитки"
        android:roundIcon="@mipmap/iconmachin"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".DrinksMainList"></activity>
        <activity android:name=".Places"></activity>
        <activity android:name=".TeaTest"></activity>
        <activity android:name=".CoffeeList"></activity>
        <activity android:name=".TeaList"></activity>
        <activity android:name=".LemonadeList"></activity>
        <activity android:name=".MyBest"></activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
    
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission." />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

</manifest>